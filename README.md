# GraphV
## Dependencies
- Ncurses (if you are in linux).
## Linux build
```
git clone https://github.com/JuanCamilo0509/graphV.git
mkdir build && cd build
cmake ../
make
```

#### Linux run
Go to the samples directory and run the binary inside it.
```
cd ../samples/
../build/graphV ./
```
